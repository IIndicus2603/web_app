{
    "builds": [{
        "src": "web_app/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "450mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "web_app/wsgi.py"
        }
    ]
}
